{
    "name": "flow-canvas-config-server",
    "private": true,
    "version": "1.0.0",
    "description": "Flow Canvas 模型能力 CONFIG 服务（/config 公开只读，/admin 密码保护的版本管理面板）",
    "type": "module",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "start": "node server.mjs",
        "hash-password": "node server.mjs --hash-password",
        "test": "node --test"
    },
    "dependencies": {
        "ajv": "^6.15.0"
    }
}
