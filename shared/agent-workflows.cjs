const AGENT_WORKFLOWS = [
    { id: 'organize', name: '素材整理', version: 1, inputs: ['project selection'], steps: ['Read selected nodes', 'Inspect ambiguous media', 'Arrange and annotate without changing source files'], tools: ['board.get_snapshot', 'asset.read', 'board.transaction.apply'], acceptance: ['Keep node identities and all references', 'No paid generation unless requested'] },
    { id: 'references', name: '多图创作', version: 1, inputs: ['ordered reference nodes', 'instruction'], steps: ['Read each reference', 'Identify explicit roles and preserved attributes', 'Create or update a generation node', 'Propose one batch', 'Review result'], tools: ['asset.read', 'model.list', 'graph.run'], acceptance: ['Preserve reference ordering', 'Retain original prompt and source links'] },
    { id: 'series', name: '系列图', version: 1, inputs: ['subject references', 'variant requirements'], steps: ['Read confirmed project constraints', 'Create variants sharing subject references', 'Confirm complete batch once', 'Compare outputs'], tools: ['memory.read', 'board.transaction.apply', 'graph.run'], acceptance: ['Use consistent subject references', 'Only regenerate rejected variants after confirmation'] },
    { id: 'image-video', name: '图片转视频', version: 1, inputs: ['image', 'motion instruction'], steps: ['Inspect image', 'Check video model duration and reference limits', 'Create connected video node', 'Confirm and execute', 'Review sampled frames'], tools: ['asset.read', 'model.list', 'graph.run'], acceptance: ['Use requested source image', 'Do not claim audio or full motion was reviewed'] },
    { id: 'review', name: '结果审阅', version: 1, inputs: ['result nodes', 'original requirements'], steps: ['Inspect result and relevant references', 'Compare explicit requirements', 'Explain observable issues', 'Offer scoped changes'], tools: ['asset.read', 'board.get_snapshot'], acceptance: ['Separate observations from inferences', 'Never regenerate automatically'] }
];
module.exports = { AGENT_WORKFLOWS };
